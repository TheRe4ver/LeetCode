#include <iostream>

using namespace std;

class Solution
{
public:
	bool isMatch(string input, string pattern)
	{
		
	}
};

void main()
{
	Solution program;
	string input = "ab", pattern = ".*";
	if (program.isMatch(input, pattern))
		cout << "Yes" << endl;
	else
		cout << "No" << endl;
}